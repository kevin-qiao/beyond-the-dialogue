// Re-exported from the domain: the rule is pure, and the settings service is
// what applies it. Kept at this path because this is where callers and tests
// have always found it.
export { validatePluginEntries } from '../core/domain/plugins'
