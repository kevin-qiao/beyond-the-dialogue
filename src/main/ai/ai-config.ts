// Re-exported from the domain: the predicate is pure, and the renderer and the
// services both need it. Kept at this path because this is where callers and
// tests have always found it.
export { isConfigured } from '../../core/domain/config'
